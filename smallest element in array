#include <stdio.h>
#include<string.h>
int main() {
	int a;
	int b[10];
	int v=-1;
	scanf("%d",&a);
	
	for(int i=0;i<a;i++)
	{
	   scanf("%d",&b[i]);
	}
	for(int j=0;j<a-1;j++)
	{
	 if(b[j]<b[j+1])
	 v=b[j];
	 else
	 v=b[j+1];
	}
	printf("%d",v);
	return 0;
}
