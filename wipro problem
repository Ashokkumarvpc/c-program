#include <stdio.h>

int main() {
	 int b[8],c[8];
	 
	 for(int i=0;i<8;i++)
	 {
	    scanf("%d",&b[i]);
	 }
	 for(int i=0;i<8;i++)
	 {
	     if(i==0)
	        c[0]=b[1];
	      else if(i==7)
	        c[7]=b[6];
	       else{
	           if(b[i+1] ^ b[i-1])
	          #include <stdio.h>

int main() {
	 int b[8],c[8];
	 
	 for(int i=0;i<8;i++)
	 {
	    scanf("%d",&b[i]);
	 }
	 for(int i=0;i<8;i++)
	 {
	     if(i==0)
	        c[0]=b[1];
	      else if(i==7)
	        c[7]=b[6];
	       else{
	           if(b[i+1] ^ b[i-1])
	                c[i]=1;
	     #include <stdio.h>

int main() {
	 int b[8],c[8];
	 
	 for(int i=0;i<8;i++)
	 {
	    scanf("%d",&b[i]);
	 }
	 for(int i=0;i<8;i++)
	 {
	     if(i==0)
	        c[0]=b[1];
	      else if(i==7)
	        c[7]=b[6];
	       else{
	           if(b[i+1] ^ b[i-1])
	                c[i]=1;
	           else
	            c[i]=0;
	               
	           }
	                
	 }
	 for(int j=0;j<8;j++){
	     printf("%d ",c[j]);
	 }
	        
	     
     
	return 0;
}      else
	            c[i]=0;
	               
	           }
	                
	 }
	 for(int j=0;j<8;j++){
	     printf("%d ",c[j]);
	 }
	        
	     
     
	return 0;
}      c[i]=1;
	           else
	            c[i]=0;
	               
	           }
	                
	 }
	 for(int j=0;j<8;j++){
	     printf("%d ",c[j]);
	 }
	        
	     
     
	return 0;
}
